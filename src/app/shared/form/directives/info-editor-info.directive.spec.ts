import { InfoEditorInfoDirective } from './info-editor-info.directive';

describe('InfoEditorInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoEditorInfoDirective();
    expect(directive).toBeTruthy();
  });
});
