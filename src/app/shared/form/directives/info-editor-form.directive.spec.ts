import { InfoEditorFormDirective } from './info-editor-form.directive';

describe('InfoEditorFormDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoEditorFormDirective();
    expect(directive).toBeTruthy();
  });
});
